<%= form_with url:  user_path(user), method: :get, local: false do |f| %>
  <div class="form-group mt-4">
    <%= f.date_field :created_at, class: 'form-control col-md-5' %>
    <%= f.submit '検索', class: 'btn btn-sm btn-primary mt-2' %>
  </div>
<% end %>